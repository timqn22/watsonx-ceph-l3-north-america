# --- Core web/backend ---
fastapi==0.139.0
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1
apscheduler==3.11.3
numpy==2.5.1
python-dotenv==1.2.2
slowapi==0.1.10

# --- Optional: real watsonx.ai Granite embeddings ---
# Install to use the WatsonxGraniteEmbedder backend.
#   pip install ibm-watsonx-ai
# ibm-watsonx-ai>=1.0.0

# --- Optional: local fallback embeddings (no credentials needed) ---
# Install to use the LocalEmbedder backend for offline dev/demo.
#   pip install sentence-transformers
# sentence-transformers>=2.2.2

# --- Dev/test ---
pytest==9.1.1
