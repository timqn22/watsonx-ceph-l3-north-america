# --- Core web/backend (always needed) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
apscheduler>=3.10.0
numpy>=1.24.0
python-dotenv>=1.0.0

# --- Optional: real watsonx.ai Granite embeddings ---
# Install to use the WatsonxGraniteEmbedder backend.
#   pip install ibm-watsonx-ai
# ibm-watsonx-ai>=1.0.0

# --- Optional: local fallback embeddings (no credentials needed) ---
# Install to use the LocalEmbedder backend for offline dev/demo.
# Recommended: provides real Granite embeddings without watsonx credentials.
sentence-transformers>=2.2.2

# --- Dev/test ---
pytest>=8.0.0
